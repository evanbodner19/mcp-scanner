"""Headless core for MCP Scanner GUIs (models, controllers, service, key store).

The browser front end lives in `mcpscanner_web`; this package provides the
UI-agnostic scan bridge and encrypted key storage it builds on.
"""

__version__ = "0.1.0"
